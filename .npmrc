# @deepseek-ai/* packages are provided by the dsh runtime; do not auto-install
# peer dependencies (several are unpublished monorepo-internal packages).
auto-install-peers=false
strict-peer-dependencies=false
